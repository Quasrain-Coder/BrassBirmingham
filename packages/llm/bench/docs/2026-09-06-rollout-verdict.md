# rollout 复核放量终验：正信号，样本不足切默认（2026-09-06）

E17/E18 配置（rolloutK=8 + deltaThreshold=2.0）落地为独立插件
`jsb-v20260906-rollout`（overrides 直写，per-seat 对照），vs jsb-v20260903
PAIRED 配对终验。

## 数据

| 批 | 配置 | 胜率 | 人均 |
|---|---|---|---|
| E17（2026-09-03） | ×20 配对 | 70%（14/20） | 116.1 vs 110.8 |
| 本次 seg1（seeds 9300-9319） | ×20 配对 | **55%（22/40）** | 114.8 vs 112.0 |
| **合计** | **×30 种子 = 60 局** | **60%（36/60）** | — |

seg2（seeds 9320-9339）运行中（rollout 版实测 ~5.2h/段）。

## 判定

- 方向为正（合计 60%、人均 +2~3），但按仓库门槛（0903 当年切默认依据
  56-63% ×500）**样本不足，不切默认**；60% 的 CI 半宽 ~12%，与 50% 无法
  区分。
- 成本实测：rollout 版 ~4.6 min/局（5.2h/40 局），×500 终验需 ~40h 串行
  或 4 段并行 ~10h——留待专门批次。
- 插件保留注册（`builtin:jsb-v20260906-rollout`），server 场景若接受
  AI 每步 ~2.3min（两行动局）可手动启用。

## 结论

②评分器路线的有效性再次由 rollout 印证（每步模拟 vs 静态估值），但收益
（+2~3 VP）远小于基线阶梯里已兑现的兜底升级（+40）。优先级排序：
兜底/架构层 > rollout 放量 > 其它 CFG 消融。
