node_modules/
dist/
*.local
.env
.env.*
!.env.example
*.db
*.db-journal
*.db-shm
*.db-wal
.DS_Store
coverage/
tmp_rules/

# 官方游戏素材随仓库分发（个人非商用）；raw/ 为下载缓存，可用 fetch-assets 重建
packages/web/public/assets/raw/

# bench 跑批输出（decisions.jsonl / games.jsonl）——跑批生成的临时日志，
# 不入库。迭代结论沉淀在 bench/docs/ITERATION.md；如需留档基线数据，写
# 汇总（分析后的指标/结论）而非原始 trace 日志。
packages/llm/bench/out/

# tsc 构建产物（vite-node/vitest 直接跑 TS 源码，编译输出不入库；
# 注意:不得让陈旧 .js 被提交——CI 无产物环境下 vitest 会优先加载它们导致解析失败）
packages/*/src/**/*.js
packages/*/src/**/*.d.ts
packages/*/test/**/*.js
packages/*/test/**/*.d.ts
packages/*/bench/**/*.js
packages/*/bench/**/*.d.ts
*.tsbuildinfo
